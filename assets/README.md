# Asset placeholders

Place your production assets here, for example:
- `hero-illustration.webp`
- `patient-portrait-01.jpg`
- `icons/`

The current build uses gradient and CSS-generated visuals so it can run immediately without external image dependencies.
