import { SectionPlaceholder } from "@/components/ui/SectionPlaceholder";

export default function Loading() {
  return (
    <main>
      <SectionPlaceholder />
      <SectionPlaceholder />
      <SectionPlaceholder />
    </main>
  );
}
